namespace PortalApp.API.Models
{
    public enum DocumentStatus
    {
        Draft, OnApproval, Approved, Rejected, Ended, Redirected, Any, Registered, InWork, OnRegistration, Template, Stopped
    }
}