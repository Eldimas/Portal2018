namespace PortalApp.API.Dtos
{
    public enum DocumentStatus
    {
        Draft, OnApproval, Approved, Rejected, Ended, Redirected, Any, Registered,
        InWork, OnRegistration, Template, Stopped
    }
}